# if you're: locked out of xorg because of gpu driver crash
# use lynx / wget / curl for downloading/browsing
# install git to control your config changes
# experimenting with config: use git: git init; git add . ; git commit -m "init"
# use tmux as a console text mode window-manager
# jq doesn't hurt either (some api tricks)
# mc & mcedit if you don't like nano/vim/etc

pkg install -y  git \
lynx \
wget \
curl \
tmux \
vim \
mc \
jq
# additional:
    # ack-grep
    # ripgrep

